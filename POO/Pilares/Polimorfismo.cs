/*
    Significa 'muitas formas' e é um dos pilares da POO
    Polimorfismo é o princípio pelo qual duas ou mais classes derivadas de uma mesma classe base
        podem invocar métodos de mesma assinatura porém com comportamentos distintos,
        especializados conforme a necessidade da classe derivada usando para tanto como parâmetro a super classe

    Permite que diferentes subclasses implementem suas próprias especializações de certos métodos, sobrescrevendo os da base
        métodos que usam a mesma assinatura

    São dois os tipos
        polimorfismo em tempo de compilação (overloading ou sobrecarga) early binding
            permite que a partir de um mesmo nome de método, diferentes parâmetros sejam passados resultando em diferentes resultados
        polimorfismo em tempo de execução (overriding ou sobrescrita) late binding
            implementado a partir de heranças e métodos virtuais

    Implementação via herança (sobrescrever), interface(diferentes métodos com mesmo nome) e abstração ()
*/
public class Polimorfismo
{

}