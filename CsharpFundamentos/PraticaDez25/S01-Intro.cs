/// <summary>
/// A plataforma .NET atual fornece um conjunto padrão de bibliotecas de classes básicas e APIs comuns a todos os aplicativos .NET
/// Multiplataforma através da CLR
/// Compilação para a IL(linguagem intermediária - bytecode) .exe ou .dll(Dynamic Link Library), posterior execução através da CLR(common language runtime) com JIT
/// Organização do código: assembly, classe, namespace = container de classes, solução = container de projetos, 
/// Interoperabilidade?
/// </summary>
public class Intro
{
}